// event

let div = document.querySelector('.block')

// div.addEventListener('click', (event) => handler(event));
// document.addEventListener('click', (event) => del(event));
// document.addEventListener('click', (event) => inner(event));

/* 
function del(event){
    event.target.remove()
}
 */

/* 
function inner(event){
    event.target.innerText = 'New Text'
}
 */
/* 
if(event.timeStamp < 500){
    console.log('OK');
} else{
    console.log('To Late');
} 
*/
/* 
function handler(event){
    if(event.altKey){
        console.log('Click + button ALT');
    }
}
 */

// document.addEventListener('keydown', (e) => console.log((e.key, e.keyCode, e.code)))
/* 
document.addEventListener('keydown', (e) => log(e))

function log(event){
    if (event.ctrlKey && event.shiftKey){
        console.log('Засчитано!')
    }
}
 */


